# 第 02 章 · 验收清单

> 对应 PPT 第 28 页 SUMMARY。

## 可运行的本地模型

- [ ] `ollama list` 至少能看到一个模型
- [ ] `ollama run qwen3.5:4b "你好"` 能在 30 秒内给出回答
- [ ] `python 测试本地模型.py` 能正常输出答案

## 一个有知识库的 Agent

- [ ] `docker compose up -d` 成功，浏览器打开 `http://localhost:3000` 能看到 Open WebUI 登录页
- [ ] 在 Open WebUI 里能选到本地模型
- [ ] **Workspace → Knowledge** 上传了至少 1 份 PDF / Markdown 资料
- [ ] 在对话里选中刚才的知识库，问一个资料里出现的问题，答案中能引用资料原文

## 部署验收清单

- [ ] 知道 Ollama 默认端口是 `11434`
- [ ] 知道 Open WebUI 通过 `OLLAMA_BASE_URL` 找 Ollama
- [ ] 关掉 Docker / 重启电脑后，再次 `docker compose up -d` 能恢复对话历史

## 过程记录完整

- [ ] 整个部署过程在 README / 工程笔记里有记录，下次重装能照着复现
